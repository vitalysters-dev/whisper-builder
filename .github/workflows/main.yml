name: Build Optimized Whisper.cpp for CoreELEC

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git make cmake g++-aarch64-linux-gnu libopenblas-dev

      - name: Clone whisper.cpp repository
        uses: actions/checkout@v4
        with:
          repository: ggml-org/whisper.cpp
          submodules: true

      # --- ВОТ ИСПРАВЛЕНИЕ: Мы возвращаем шаг создания Toolchain файла ---
      - name: Create Toolchain File
        run: |
          echo 'set(CMAKE_SYSTEM_NAME Linux)' > aarch64-toolchain.cmake
          echo 'set(CMAKE_SYSTEM_PROCESSOR aarch64)' >> aarch64-toolchain.cmake
          echo 'set(CMAKE_C_COMPILER aarch64-linux-gnu-gcc)' >> aarch64-toolchain.cmake
          echo 'set(CMAKE_CXX_COMPILER aarch64-linux-gnu-g++)' >> aarch64-toolchain.cmake
          echo 'set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM NEVER)' >> aarch64-toolchain.cmake
          echo 'set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY ONLY)' >> aarch64-toolchain.cmake
          echo 'set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE ONLY)' >> aarch64-toolchain.cmake
          echo 'set(CMAKE_FIND_ROOT_PATH_MODE_PACKAGE ONLY)' >> aarch64-toolchain.cmake

      - name: Compile with CMake and OpenBLAS
        run: |
          mkdir build && cd build
          cmake .. \
            -DCMAKE_TOOLCHAIN_FILE=../aarch64-toolchain.cmake \
            -DCMAKE_CXX_FLAGS="-O3 -mtune=cortex-a53" \
            -DWHISPER_OPENBLAS=ON \
            -DWHISPER_BUILD_TESTS=OFF
          make -j$(nproc) main

      - name: Package Executable and Dependencies
        run: |
          mkdir package
          cp build/bin/main package/
          # Используем ldd для поиска и копирования всех зависимостей
          aarch64-linux-gnu-ldd build/bin/main | grep "=> /" | awk '{print $3}' | xargs -I '{}' cp '{}' package/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: optimized-whisper-package
          path: package
